# core.txt — Essential runtime dependencies
# Install: pip install -r core.txt
#
# Purpose: Everything needed to run the AIRO pipeline at runtime.
# No ML frameworks, no LLM stack, no dev tools.

# Data I/O 
pandas>=2.2.0           # Core data manipulation (DataFrames, CSV/parquet I/O)
numpy>=1.26.0           # Numerical operations (required by pandas, sklearn, shap)
pyarrow>=17.0.0         # Fast parquet read/write for train/val/test splits

# Validation & config 
pydantic>=2.9.0         # Data validation; also a hard transitive dep of langgraph
pyyaml>=6.0.2           # Parsing YAML experiment configs generated by Config Agent
python-dotenv>=1.0.0    # Loading GROQ_API_KEY and all AIRO_* env vars

# Templating & reporting 
jinja2>=3.1.0           # Renders the Markdown experiment report template
markdown>=3.7.0         # Converts Markdown → HTML for WeasyPrint PDF pipeline

# CLI & logging 
rich>=13.8.0            # Formatted CLI output: tables, panels, progress bars
typer>=0.12.0           # CLI entrypoint: `airo --dataset ... --task ...`
loguru>=0.7.0           # Structured logging across all agents
tenacity>=9.0.0         # Retry decorator on Claude API calls (llm_tools.py)

# Frontend dashboard 
streamlit>=1.39.0       # Web dashboard: 4 pages (run, trace, leaderboard, report)
plotly>=5.24.0          # Interactive charts in metric_chart.py component
